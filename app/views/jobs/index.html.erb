<% provide(:title, 'All Jobs') %>
<h1>All Jobs</h1>

<%= paginate @jobs, window: 1 %>
<ul class="jobs">
  <% @jobs.each do |job| %>
    <li>
      <%= link_to job.title, job %>
    </li>
  <% end %>
</ul>
<%= paginate @jobs, window: 1 %>