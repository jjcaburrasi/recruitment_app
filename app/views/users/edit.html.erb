<%= form_with(model: @user) do |form| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this stage from being saved:</h2>
      <ul>
        <% @user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %><br>
    <%= form.text_field :name %><br><br>
  </div>

  <div class="field">
    <%= form.label :email %><br>
    <%= form.text_field :email %><br><br>
  </div>

  <div class="field">
    <%= form.label :description %><br>
    <%= form.text_area :description %><br><br>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>